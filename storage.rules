rules_version = '2';
// Career Compass / JobVerse: only the signed-in user can write to their own resume folder.
service firebase.storage {
  match /b/{bucket}/o {
    match /resumes/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
